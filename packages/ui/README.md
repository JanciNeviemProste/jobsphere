# @jobsphere/ui

**Status: PLACEHOLDER / DEPRECATED**

This package is currently a placeholder for future shared UI components.

## Current State

- No actual components implemented
- Not used by any apps in the monorepo
- Removed from Next.js `transpilePackages`

## Future Plans

This package is reserved for:
- Shared React components across multiple apps
- Common UI primitives
- Design system components

## If You Need This

To activate this package:
1. Add actual component files
2. Add build configuration (tsup, tsconfig, etc.)
3. Add to `transpilePackages` in apps/web/next.config.js
4. Update package.json with proper exports
