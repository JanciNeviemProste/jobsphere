/**
 * Re-exports from prisma.ts for backwards compatibility.
 * New code should import from '@/lib/prisma' directly.
 */
export { prisma } from './prisma'
export { prisma as default } from './prisma'
