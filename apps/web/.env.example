# ==========================================
# JobSphere Web App - Environment Variables
# ==========================================
# Copy to .env.local for local development

# ----- PUBLIC (Exposed to Browser) -----
# App URL (your Vercel domain)
NEXT_PUBLIC_APP_URL=https://jobsphere-khaki.vercel.app

# ----- SERVER-ONLY (Never exposed to browser) -----
# NextAuth
# Generate secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-super-secret-key-min-32-chars-generate-with-openssl
NEXTAUTH_URL=https://jobsphere-khaki.vercel.app

# Database (Vercel Postgres)
# Get from Vercel Dashboard: Storage → Postgres → .env.local tab
DATABASE_URL=postgresql://user:pass@host:5432/db?sslmode=require
POSTGRES_PRISMA_URL=postgresql://user:pass@host:5432/db?sslmode=require&pgbouncer=true
POSTGRES_URL_NON_POOLING=postgresql://user:pass@host:5432/db?sslmode=require

# Google OAuth (for NextAuth)
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ----- OPTIONAL -----
# Stripe (for billing - optional for now)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...

# Redis (for caching - optional for now)
REDIS_URL=redis://default:pass@host:6379

# Analytics (optional)
NEXT_PUBLIC_POSTHOG_KEY=
NEXT_PUBLIC_POSTHOG_HOST=https://app.posthog.com

# Sentry (optional)
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=
